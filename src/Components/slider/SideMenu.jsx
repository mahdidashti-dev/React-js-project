import React, { useState } from 'react';

export default function SideMenu({ onSubmit }) {
  const [showClose, setShowClose] = useState(false);
  const toggleTrueFalse = () => setShowClose(!showClose);

  onSubmit({ showClose })
  { console.log(showClose) }

  
  return (

    <div id="toggler">
      <button id="toggler" onClick={e=>toggleTrueFalse(e) }> <i className="material-icons"> list  </i> </button>
    </div>

  )
}















































































































